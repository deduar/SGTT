{% extends 'tematwigcsg/plantilla.html.twig' %}

{% block body %}
    <h1>Excepcion creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary" value="Crear" />
    {{ form_end(form) }}

{% endblock %}
