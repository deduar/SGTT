<div id="inner-user">
    {% if (app.session.get('id') != null) %}
    <ul id="user-links" >
        <li>         
            <a href="{{ path('excepcion_index') }}" title="Home">
                <i class="glyphicon glyphicon-home"></i>Home
            </a>
        </li>
        <li> 
            <a href="{{ path('empleado_show',{ 'id': empleado.id }) }}" class="dropdown-toggle" data-toggle="dropdown" role="button"  >
                <i class="glyphicon glyphicon-user"></i>
                {{persona.nombre}} {{persona.apellido}}
            </a>
        </li>
        <li>         
            <a href="{{ path('empleado_logout') }}" title="Cerrar Sesión">
                <i class="glyphicon glyphicon-off"></i>Salir
            </a>
        </li>
    </ul>
    {% endif %}
</div>